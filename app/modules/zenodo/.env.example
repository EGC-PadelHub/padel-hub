ZENODO_ACCESS_TOKEN=<GET_ACCESS_TOKEN_IN_ZENODO>
# Optional: point to local fakenodo instead of real Zenodo API
# Example: http://127.0.0.1:5055/api/deposit/depositions
FAKENODO_URL=
# Optional: hostname for fakenodo in Docker (default: 127.0.0.1, production: web)
# This is automatically set in docker-compose.prod.yml files
FAKENODO_HOSTNAME=